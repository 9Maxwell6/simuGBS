#ifndef STRING_UTILS_H
#define STRING_UTILS_H

#include <string>
#include <vector>
#include <algorithm>
using namespace std;

std::vector<std::string> split( std::string const& original, char separator );

int digitNumInString(std::string str);

bool is_bitString(string str);




#endif
